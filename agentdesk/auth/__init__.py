# auth — JWT authentication utilities for AgentDesk.
#
# Modules:
#   hashing.py      — bcrypt password hashing / verification
#   jwt_utils.py    — access and refresh token creation + decoding
#   dependencies.py — FastAPI Depends: get_current_user()
